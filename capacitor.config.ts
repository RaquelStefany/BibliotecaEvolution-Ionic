import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.biblioteca.evolution',
  appName: 'BibliotecaEvolution',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
